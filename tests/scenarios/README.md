# Scenarios

Business-flow and multi-step scenario test assets belong here.

Scenario assets should map back to entries in `tests/plans/` and forward to normalized results in `tests/results/`.

This directory may also hold golden-dataset style scenario corpora for the
cycling-agent product. Those corpora should encode stable requests and expected
outcomes for clarification, recommendation, fallback, and no-match behavior.
