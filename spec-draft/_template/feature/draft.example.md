# `<feature-title>`

## 目标/Goal

说明这个功能想解决什么业务问题，以及预期结果是什么。

最好添加通用的背景信息，放在 rules 当中，比如项目架构

fe：提供用户注册/登录过程，需要支持

## 流程

用 3 到 6 步描述主流程：

1. `<actor>` 发起 `<action>`
2. 系统执行 `<behavior>`
3. 用户得到 `<result>`

## 接口

- `<METHOD>` /`<domain>`/`<feature>`/`<action>`
- `<METHOD>` /`<domain>`/`<feature>`/`<action>`

## 规则

- <rule-1>
- <rule-2>

## 备注

- <remark>
