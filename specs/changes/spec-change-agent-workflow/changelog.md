# Changelog

## 0.1.0

- Added the mixed request lifecycle workflow from request intake through promote and archive.
- Split spec intake from change ownership with `spec-draft-agent` and `spec-agent`.
- Added dedicated `architecture-agent` plus separate frontend, backend, and migration execution agents.
- Added `routing-summary.json`, dual review gates, and route-specific implementation reports.
