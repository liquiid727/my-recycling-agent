# Checks

Repository validation commands such as `xcli check`-style gates belong here.

Current repository-owned checks include:

- `validate-spec-driven-lifecycle.mjs`: workflow and manifest lifecycle contract
- `validate-cycling-agent-assets.mjs`: four-layer asset map and golden-dataset baseline contract
